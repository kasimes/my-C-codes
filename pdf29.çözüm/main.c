#include <stdio.h>
#include <stdlib.h>

int main()
{int sayi,i,kar,iki;
    printf("lütfen bir sayı giriniz\n");
    i=1;
    kar=0;
    iki=0;
    while(sayı<5&&sayı!=0)
    {
        i=i*sayı;
        sayı--;
    }
    printf("%d");
    if(sayı<=10&&sayı>=5);

    return 0;
}
