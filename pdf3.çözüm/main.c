#include <stdio.h>
#include <stdlib.h>

int main()
{
    int sayac,bitir,carp;
    sayac=4;
    while(sayac*sayac<1000)
    {
        if(sayac*sayac<1000)
        {
            carp=sayac*sayac;
            printf("%d",sayac);
        }
        else{
            printf("bitir\n");
        }
        sayac++;
    }
  printf("%d",oran);
    return 0;
}
